# VertexHermesProxy

A proxy service for Vertex/Hermes integrations.

## Setup

1. Clone the repository: `git clone git@github.com:FireFlyDeveloper/VertexHermesProxy.git`
2. Configure environment variables (refer to `.env.example`).
3. Run the service.

## Documentation

(Documentation content to be added.)
